# Environment
.env

# Vector database — regenerate locally using setup scripts
chroma_db/

# Transcripts and processed files — regenerate locally
transcripts/
processed/

# Personal user data — never commit
favourites.json
prayer_journal.json
user_name.json
scheduler_log.json
scheduler_log.txt

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db

# Eval results (commit baselines manually when ready)
eval/results/
